(function () {
  const records = {
    'TSK-00081': {
      subject: 'Confirm deployment availability', status: 'assigned', context: 'Tana River Flood Response — Wave 3', priority: 'Urgent', type: 'Deployment response', geo: 'Tana River County', assigned: '31 Aug 2026 · 10:42', start: '14 Sep 2026 · 08:00', end: '25 Sep 2026 · 17:00', due: '2 Sep 2026 · 17:00', response: '2 Sep 2026 · 17:00', expected: '96 hours', actual: '—', progress: 0,
      description: 'Review the approved Terms of Reference and confirm whether you can take part in the full deployment period from 14–25 September.', work: 'Hola Branch Office', address: 'Hola, Tana River County', meeting: 'Registration desk · 07:45', travel: 'Report using the approved deployment travel plan. Bring identification and your volunteer card.', contact: 'Amina Noor · +254 700 000 001', coordinator: { name: 'Amina Noor', user: 'amina.noor@redcross.or.ke', initials: 'AN' }, openQuestion: true,
      checklist: [['Read the Terms of Reference', true, false], ['Send your availability response', true, false]], files: [['Terms of Reference — v1.4', 'Approved · 1.8 MB']], submitted: '—', closed: '—', completion: '—', outcome: '—', rework: '0', returned: '—',
      thread: [
        { type: 'assigned', author: 'Amina Noor', role: 'coordinator', at: '31 Aug 2026 · 10:42', note: 'Review the approved Terms of Reference and confirm whether you can take part in the full deployment period.' },
        { type: 'question', author: 'You', role: 'volunteer', at: '1 Sep 2026 · 09:15', note: 'Will transport from Nairobi to Hola be arranged, or should I report directly to the branch?' }
      ]
    },
    'TSK-00076': {
      subject: 'Complete safeguarding refresher', status: 'accepted', context: 'Volunteer readiness', priority: 'High', type: 'Required training', geo: 'Nairobi Branch', assigned: '29 Aug 2026 · 08:15', start: '1 Sep 2026', end: '4 Sep 2026', due: '4 Sep 2026 · 23:59', response: 'Accepted 30 Aug 2026', expected: '2 hours', actual: '—', progress: 60,
      description: 'Complete the safeguarding refresher course and pass the short knowledge check to keep your active volunteer record current.', work: 'Online learning portal', address: 'Remote', meeting: '—', travel: 'No travel required.', contact: 'Nairobi Branch Volunteer Office', coordinator: { name: 'Jane Njeri', user: 'jane.njeri@redcross.or.ke', initials: 'JN' }, openQuestion: false,
      checklist: [['Complete modules 1–3', true, true], ['Complete modules 4–5', true, false], ['Pass the knowledge check', true, false]], files: [['Safeguarding learner guide', 'PDF · 940 KB']], submitted: '—', closed: '—', completion: '—', outcome: '—', rework: '0', returned: '—',
      thread: [
        { type: 'assigned', author: 'Jane Njeri', role: 'coordinator', at: '29 Aug 2026 · 08:15', note: 'Please complete the refresher before the current certificate expires.' },
        { type: 'accepted', author: 'You', role: 'volunteer', at: '30 Aug 2026 · 10:04', note: '' },
        { type: 'question', author: 'You', role: 'volunteer', at: '31 Aug 2026 · 14:20', note: 'Can I complete the knowledge check on my phone?' },
        { type: 'answer', author: 'Jane Njeri', role: 'coordinator', at: '31 Aug 2026 · 15:02', note: 'Yes. Open the learning portal in your browser; the assessment works on mobile.' },
        { type: 'progress', author: 'You', role: 'volunteer', at: '1 Sep 2026 · 08:40', note: 'Modules 1–3 are complete. I will finish the remaining modules this evening.', proof: 'module-progress.jpg' }
      ]
    },
    'TSK-00084': {
      subject: 'Upload identification renewal', status: 'assigned', context: 'Your volunteer record', priority: 'Normal', type: 'Record update', geo: 'Nairobi Branch', assigned: '1 Sep 2026 · 09:00', start: '1 Sep 2026', end: '20 Sep 2026', due: '20 Sep 2026 · 23:59', response: '8 Sep 2026 · 17:00', expected: '—', actual: '—', progress: 0,
      description: 'Upload a clear copy of your renewed identification document. Both sides are required when the document contains information on the reverse.', work: 'Volunteer portal', address: 'Online', meeting: '—', travel: 'No travel required.', contact: 'Nairobi Branch Volunteer Office', coordinator: { name: 'Jane Njeri', user: 'jane.njeri@redcross.or.ke', initials: 'JN' }, openQuestion: false,
      checklist: [['Upload the front of the identification', true, false], ['Upload the reverse where applicable', false, false]], files: [['Accepted identity-document guide', 'PDF · 240 KB']], submitted: '—', closed: '—', completion: '—', outcome: '—', rework: '0', returned: '—',
      thread: [{ type: 'assigned', author: 'Jane Njeri', role: 'coordinator', at: '1 Sep 2026 · 09:00', note: 'Please upload the renewed identification before the current record expires.' }]
    },
    'TSK-00063': {
      subject: 'Submit household screening return', status: 'submitted', context: 'Garissa Nutrition Outreach', priority: 'High', type: 'Field evidence', geo: 'Garissa County', assigned: '23 Aug 2026 · 16:20', start: '24 Aug 2026', end: '31 Aug 2026', due: '31 Aug 2026 · 17:00', response: 'Accepted 24 Aug 2026', expected: '12 hours', actual: '12 hours', progress: 100,
      description: 'The household screening return and supporting photographs have been submitted to the team leader for verification.', work: 'Garissa outreach wards', address: 'Garissa County', meeting: 'Garissa Branch Office', travel: 'Use assigned team transport.', contact: 'David Mwangi · Team Leader', coordinator: { name: 'David Mwangi', user: 'david.mwangi@redcross.or.ke', initials: 'DM' }, openQuestion: false,
      checklist: [['Complete screening return', true, true], ['Check referral totals', true, true], ['Attach field evidence', true, true], ['Submit to team leader', true, true]], files: [['Household screening template', 'XLSX · 112 KB'], ['Field activity evidence', '12 photographs']], submitted: '31 Aug 2026 · 16:42', closed: '—', completion: 'Awaiting coordinator sign-off', outcome: '—', rework: '0', returned: '—',
      thread: [
        { type: 'assigned', author: 'David Mwangi', role: 'coordinator', at: '23 Aug 2026 · 16:20', note: 'Complete and return the household screening register with field evidence.' },
        { type: 'accepted', author: 'You', role: 'volunteer', at: '24 Aug 2026 · 07:40', note: '' },
        { type: 'progress requested', author: 'David Mwangi', role: 'coordinator', at: '28 Aug 2026 · 09:30', note: 'Please share the number of households completed so far.' },
        { type: 'progress', author: 'You', role: 'volunteer', at: '28 Aug 2026 · 10:05', note: 'We have completed 84 of the planned 120 households.' },
        { type: 'submitted', author: 'You', role: 'volunteer', at: '31 Aug 2026 · 16:42', note: 'Screening return and supporting photographs submitted.', proof: 'screening-evidence.jpg' }
      ]
    },
    'TSK-00014': {
      subject: 'Attend branch deployment briefing', status: 'completed', context: 'Garissa Drought Response', priority: 'Normal', type: 'Deployment briefing', geo: 'Garissa County', assigned: '29 Jan 2026 · 11:00', start: '2 Feb 2026 · 09:00', end: '2 Feb 2026 · 12:00', due: '2 Feb 2026 · 12:00', response: 'Accepted 30 Jan 2026', expected: '3 hours', actual: '3 hours', progress: 100,
      description: 'Attend the mandatory safety, safeguarding and field coordination briefing before deployment.', work: 'Garissa Branch Office', address: 'Garissa Town', meeting: 'Main training room', travel: 'Report 15 minutes before the briefing.', contact: 'Garissa Branch Office', coordinator: { name: 'David Mwangi', user: 'david.mwangi@redcross.or.ke', initials: 'DM' }, openQuestion: false,
      checklist: [['Attend safety briefing', true, true], ['Sign attendance record', true, true]], files: [['Deployment briefing pack', 'PDF · 2.1 MB']], submitted: '2 Feb 2026 · 12:08', closed: '2 Feb 2026 · 12:30', completion: 'Attendance verified by the coordinator.', outcome: 'Briefing completed successfully.', rework: '0', returned: '—',
      thread: [
        { type: 'assigned', author: 'David Mwangi', role: 'coordinator', at: '29 Jan 2026 · 11:00', note: 'Attend the mandatory pre-deployment briefing.' },
        { type: 'accepted', author: 'You', role: 'volunteer', at: '30 Jan 2026 · 09:12', note: '' },
        { type: 'submitted', author: 'You', role: 'volunteer', at: '2 Feb 2026 · 12:08', note: 'Briefing attended and attendance record signed.' },
        { type: 'completed', author: 'David Mwangi', role: 'coordinator', at: '2 Feb 2026 · 12:30', note: 'Attendance verified.' }
      ]
    }
  };

  const name = new URLSearchParams(location.search).get('task') || 'TSK-00081';
  const record = records[name] || records['TSK-00081'];
  const set = (id, value) => { const node = document.getElementById(id); if (node) node.textContent = value || '—'; };
  const isOpen = !['completed', 'cancelled', 'declined', 'reassigned'].includes(record.status);
  const toast = document.getElementById('record-toast');
  const escapeHtml = (value) => String(value || '').replace(/[&<>"']/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' })[character]);

  function notify(message) {
    toast.textContent = message;
    toast.classList.add('show');
    clearTimeout(window.__taskToast);
    window.__taskToast = setTimeout(() => toast.classList.remove('show'), 2600);
  }

  function scrollToConversation() {
    document.getElementById('thread').scrollIntoView({ behavior: 'smooth', block: 'start' });
    setTimeout(() => document.getElementById('record-chat-note').focus(), 450);
  }

  function renderQuestionState() {
    document.getElementById('record-question-state').hidden = !record.openQuestion;
    document.getElementById('record-open-question').hidden = !record.openQuestion;
  }

  const labels = {
    assigned: 'Task assigned', accepted: 'Task accepted', question: 'Clarification', answer: 'Coordinator reply',
    'progress requested': 'Progress requested', progress: 'Progress update', submitted: 'Submitted for sign-off',
    completed: 'Task completed', returned: 'Work returned', cancelled: 'Task cancelled', declined: 'Task declined',
    reassigned: 'Task reassigned', escalated: 'Escalated', reminded: 'Reminder'
  };

  function renderThread() {
    const thread = document.getElementById('record-thread');
    thread.innerHTML = '';
    record.thread.forEach((entry) => {
      const row = document.createElement('article');
      row.className = `task-chat-entry ${entry.role === 'volunteer' ? 'from-volunteer' : 'from-coordinator'} entry-${entry.type.replace(/\s+/g, '-')}`;
      const initials = entry.role === 'volunteer' ? 'AM' : record.coordinator.initials;
      row.innerHTML = `<span class="task-chat-avatar">${escapeHtml(initials)}</span><div class="task-chat-bubble"><div><strong>${escapeHtml(entry.author)}</strong><small>${escapeHtml(labels[entry.type] || entry.type)} · ${escapeHtml(entry.at)}</small></div>${entry.note ? `<p>${escapeHtml(entry.note)}</p>` : ''}${entry.proof ? `<a href="#" class="task-chat-proof" data-proof>${escapeHtml(entry.proof)}</a>` : ''}</div>`;
      row.querySelector('[data-proof]')?.addEventListener('click', (event) => { event.preventDefault(); notify('Attachment preview opened.'); });
      thread.appendChild(row);
    });
    set('record-message-count', record.thread.length);
  }

  document.title = `${record.subject} — Kenya Red Cross`;
  set('task-breadcrumb', name); set('record-reference', name); set('record-title', record.subject); set('record-context', record.context); set('record-description', record.description);
  set('record-start', record.start); set('record-end', record.end); set('record-due', record.due); set('record-response', record.response); set('record-expected', record.expected); set('record-actual', record.actual);
  set('record-work-name', record.work); set('record-work-address', record.address); set('record-meeting', record.meeting); set('record-travel', record.travel); set('record-contact', record.contact);
  set('record-priority', record.priority); set('record-type', record.type); set('record-assigned', record.assigned); set('record-geo', record.geo); set('aside-progress', `${record.progress}%`);
  set('record-submitted', record.submitted); set('record-closed', record.closed); set('record-completion', record.completion); set('record-outcome', record.outcome); set('record-rework', record.rework); set('record-return', record.returned);
  set('record-coordinator-name', record.coordinator.name); set('record-coordinator-initials', record.coordinator.initials); set('record-author-link', record.coordinator.name);

  ['record-status', 'aside-status'].forEach((id) => {
    const node = document.getElementById(id);
    node.textContent = record.status;
    node.className = `task-status-label ${record.status}`;
  });

  const progress = document.getElementById('record-progress');
  progress.value = record.progress; progress.disabled = !isOpen;
  set('record-progress-output', `${record.progress}%`);
  progress.addEventListener('input', () => set('record-progress-output', `${progress.value}%`));

  const checklist = document.getElementById('record-checklist');
  checklist.innerHTML = '';
  record.checklist.forEach((item) => {
    const label = document.createElement('label');
    label.className = 'task-check';
    label.innerHTML = `<input type="checkbox" ${item[2] ? 'checked' : ''} ${record.status === 'submitted' || record.status === 'completed' ? 'disabled' : ''}><span><strong>${item[0]} ${item[1] ? '<em>Required</em>' : ''}</strong><small>${item[2] ? 'Completed' : 'Not completed'}</small></span>`;
    checklist.appendChild(label);
  });
  set('record-checklist-count', `${record.checklist.filter((item) => item[1] && item[2]).length} of ${record.checklist.filter((item) => item[1]).length} required items`);

  const files = document.getElementById('record-files');
  files.innerHTML = '';
  record.files.forEach((file) => {
    const link = document.createElement('a');
    link.className = 'task-file'; link.href = '#';
    link.innerHTML = `<i>FILE</i><span><strong>${file[0]}</strong><small>${file[1]}</small></span><b>Open</b>`;
    link.addEventListener('click', (event) => { event.preventDefault(); notify('Briefing file opened.'); });
    files.appendChild(link);
  });

  renderThread(); renderQuestionState();

  const primary = document.getElementById('record-primary');
  const secondary = document.getElementById('record-secondary');
  const message = document.getElementById('record-message');
  if (record.status === 'assigned') { primary.textContent = 'Accept task'; secondary.textContent = 'Decline task'; }
  else if (record.status === 'accepted') { primary.textContent = 'Submit for sign-off'; secondary.textContent = 'Report progress'; }
  else if (record.status === 'submitted') { primary.textContent = 'Awaiting coordinator sign-off'; primary.disabled = true; secondary.textContent = 'View evidence'; }
  else { primary.textContent = 'Completed'; primary.disabled = true; secondary.textContent = 'View activity'; message.textContent = 'Conversation closed'; message.disabled = true; }

  primary.addEventListener('click', () => notify(record.status === 'assigned' ? 'Task accepted and an accepted update was recorded.' : 'Complete all required checklist items before submitting.'));
  secondary.addEventListener('click', () => {
    if (record.status === 'assigned') notify('Declining this assigned task requires a reason.');
    else if (record.status === 'accepted') { document.getElementById('record-progress-note').scrollIntoView({ behavior: 'smooth', block: 'center' }); setTimeout(() => document.getElementById('record-progress-note').focus(), 450); }
    else document.getElementById(record.status === 'submitted' ? 'checklist' : 'thread').scrollIntoView({ behavior: 'smooth' });
  });
  message.addEventListener('click', scrollToConversation);
  document.getElementById('record-author-link').addEventListener('click', scrollToConversation);

  document.getElementById('record-send-question').addEventListener('click', () => {
    const input = document.getElementById('record-chat-note');
    const proof = document.getElementById('record-chat-proof');
    const note = input.value.trim();
    if (!isOpen) return notify('Questions cannot be added to a closed task.');
    if (!note) { input.focus(); return notify('Write your clarification before sending.'); }
    record.thread.push({ type: 'question', author: 'You', role: 'volunteer', at: 'Now', note, proof: proof.files[0]?.name || '' });
    record.openQuestion = true; input.value = ''; proof.value = '';
    renderThread(); renderQuestionState();
    notify(`${record.coordinator.name} has been notified of your clarification.`);
  });

  document.getElementById('record-report-progress').addEventListener('click', () => {
    const input = document.getElementById('record-progress-note');
    const note = input.value.trim();
    if (!isOpen) return notify('Progress cannot be added to a closed task.');
    if (!note) { input.focus(); return notify('Add a short progress note before sending.'); }
    record.progress = Number(progress.value);
    record.thread.push({ type: 'progress', author: 'You', role: 'volunteer', at: 'Now', note });
    set('aside-progress', `${record.progress}%`); input.value = '';
    renderThread(); notify('Progress update sent to the coordinator.');
  });
})();
