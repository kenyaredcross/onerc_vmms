[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://docs.frappe.io/framework/user/en/database-migrations
vmmsx.patches.register_modules
# A patch runs once per site, by name — so a module added later needs its own
# entry here, not a re-run of the one above. See the patch's docstring.
vmmsx.patches.register_member_modules
vmmsx.patches.register_volunteer_module
vmmsx.patches.register_deployment_module
vmmsx.patches.register_stipend_module
vmmsx.patches.register_content_module
vmmsx.patches.register_notification_module
vmmsx.patches.register_task_module
vmmsx.patches.register_branch_module
vmmsx.patches.register_registration_module
vmmsx.patches.register_events_module
# VMMS HR, and the six doctypes the Job Opening screening port brings with it.
# Eleventh instance of the pattern the patch's own docstring names.
vmmsx.patches.register_hr_module

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated
vmmsx.patches.consolidate_person_facts_on_red_profile
vmmsx.patches.unpublish_legacy_volunteer_web_form
vmmsx.patches.setup_member_module
vmmsx.patches.install_membership_scope_role
vmmsx.patches.setup_volunteer_module
vmmsx.patches.install_volunteer_scope_role
vmmsx.patches.setup_volunteer_application_module
vmmsx.patches.install_certificate_print_role
vmmsx.patches.install_self_service_roles
vmmsx.patches.setup_deployment_module
vmmsx.patches.install_deployment_scope_roles
vmmsx.patches.setup_stipend_module
vmmsx.patches.install_stipend_scope_roles
vmmsx.patches.setup_buzz_seam
vmmsx.patches.setup_content_module
vmmsx.patches.setup_notification_module
vmmsx.patches.setup_task_module
vmmsx.patches.setup_branch_module
# Last, because a society's seed writes to the settings fields every patch
# above installs. Does nothing unless the site names a seed in its own config.
vmmsx.patches.seed_society
# And after it, for the reason its own docstring gives: the patch above is
# already in every existing site's Patch Log and a patch runs once per site by
# name, so everything added to a seed since — the scope-role settings the
# console's tab gating reads, and the operations companion that was never run
# by a patch at all — arrives through a new name or not at all.
vmmsx.patches.reseed_society_with_operations
# Places the national-level approver the routing design has always depended on
# and was never seeded, then re-opens the queue for applications that resolved
# nobody at the time they were submitted. See the patch's own docstring: without
# it, every application outside one region was admitted to nobody at all.
vmmsx.patches.repair_approval_routing
# Which role, if any, may open onerc_sms's own campaign builder — the one
# setting vmmsx owns for a doctype it does not. See the patch's own docstring.
vmmsx.patches.install_sms_scope_role
# VMMS Terms of Reference became submittable, so every record written before
# that is a draft — and a draft takes no deployments. See the patch's own
# docstring for why this sets the docstatus rather than running submit().
vmmsx.patches.submit_existing_terms_of_reference
# The roster moved off VMMS Deployment.participants and onto its own document,
# VMMS Deployment Assignment. Without this every existing deployment reads as
# having nobody on it, and the ownership rule starts refusing time logs from
# people who demonstrably served. See the patch's own docstring.
vmmsx.patches.migrate_participants_to_assignments
# Two starting vocabularies: the day-spans a weekly availability grid is built
# from, and the register a mission's approach is written from. Both open sets a
# society edits from the moment they land. See the patch's own docstring for why
# the availability windows already seeded are not the ones the grid can use.
vmmsx.patches.seed_scheduling_vocabularies
# Dense but fictional presentation activity for the explicitly configured
# Tanzania demo site: people, memberships, projects, TORs, deployments, tasks,
# notifications and historical email records. Other sites are left untouched.
vmmsx.patches.seed_tanzania_presentation_scale
# Which identity documents a volunteer must produce, and the age this society
# treats as adult. Custom Fields vmmsx owns on core's Identification Type and
# National Society Settings; all four ship empty, and empty narrows nothing.
vmmsx.patches.install_identity_document_rules
# The starting vocabulary for how a guardian's consent was checked. An open set
# a society edits from the moment it lands.
vmmsx.patches.seed_guardian_verification_methods
# The programme of work moved onto ERPNext's own Project and VMMS Project was
# retired. Migrates the records rather than deleting them, and repoints every
# terms of reference written under one. See the patch's own docstring for why the
# links are repointed with db.set_value and why summary and notes swap homes.
vmmsx.patches.adopt_standard_project
# A mission's resource lines now count in ERPNext's UOM register rather than in
# free text. Matches every value already written onto a real UOM, and creates the
# ones a society used that ERPNext does not ship.
vmmsx.patches.link_tor_resource_units
# Every deployment now names a responsible coordinator and carries its period as
# datetimes. Fills both in from what each record already says, so a register
# written before those fields existed is not one nobody can save. See the
# patch's own docstring for why the owner is the honest answer and why it writes
# columns rather than saving documents.
vmmsx.patches.complete_deployment_records
# Every task now carries its deadline as a datetime and a priority. Fills both
# in from what each record already says, so the overdue sweep and the register's
# ordering see the whole register rather than only rows saved since.
vmmsx.patches.complete_task_records
# The metadata a deleted doctype leaves behind — a naming-series Property Setter,
# a Custom Field targeting it. The sweep runs at the end of adopt_standard_project
# now; this is the same sweep under a name the Patch Log has not seen, for sites
# where that patch had already run. See its own docstring.
vmmsx.patches.purge_retired_project_metadata
# A declaration's wording is now a submitted version record rather than a field
# somebody can edit. Publishes what each declaration is showing today as its
# first version, and points every acceptance already recorded at the version it
# was made against. See the patch's own docstring for why published_on is the
# declaration's modified date and not the date of the deploy.
vmmsx.patches.publish_declaration_versions
# Whether somebody has a disability, and what would help — two Custom Fields on
# core's Red Profile. Volunteer registration asks for the first and will not be
# submitted without an answer; "Prefer not to say" is one. See the patch's own
# docstring for why it sits on the spine rather than on the volunteer record.
vmmsx.patches.install_disability_fields
# The public FAQ has a page now. Points the fifth top-navigation slot at it on a
# site seeded before that page existed — and only when the slot is still
# entirely empty, so nothing a society wrote is touched.
vmmsx.patches.link_faq_in_navigation
# Which ways of paying a society will take a membership fee, and which one an
# applicant chose. A table on core's National Society Settings seeded from the
# payments app's own gateway register, and one field on the membership. See the
# patch's own docstring for why the choice is the society's and the applicant's
# rather than a single setting in another app.
vmmsx.patches.install_membership_payment_methods
# *Which* disability, on top of whether. Points a Table MultiSelect on core's
# Red Profile at core's own Disability register — which shipped with no reader
# in this app — grants All read on it, and seeds the Washington Group domains as
# a starting vocabulary. The Select installed above stays the required question
# and stays the only thing that can hold "Prefer not to say".
vmmsx.patches.install_disability_vocabulary
# What somebody has already done — profession, education, training, experience,
# licences, driving licence and references — as seven fields on core's Red
# Profile, with the two registers they need seeded. All optional; no completion
# rule moved. See the patch's own docstring for why they sit on the spine.
vmmsx.patches.install_background_fields
# A real Other option in the shared Profession register and the applicant's
# free-text description beside it on Red Profile. Registration requires the
# description only when Other is selected; the shared profile remains optional.
vmmsx.patches.install_other_profession
# Five questions a society may want and this product must not assume — blood
# group, health notes, insurance cover, internet access, dependants. Seeded
# **inactive** into the form builder as worked examples, so nobody is asked one
# until an administrator switches it on.
vmmsx.patches.seed_optional_questions
