frappe.ready(function () {
	// Nothing to bind. The two-doctype write and the routed-only guardrail are
	// server-side, on the document: see vmmsx/registration/services/intake.py
	// and VMMSMembership.validate_source().
});
